# Canonical Lifty Supabase (wabdd…) — same Auth issuer as backend + admin + mobile
# https://wabddbkwugepkwrgzhpk.supabase.co
# Legacy tránsito-only project (ykchnss…) is retired after cutover.
VITE_SUPABASE_URL=https://wabddbkwugepkwrgzhpk.supabase.co
VITE_SUPABASE_ANON_KEY=
# Backend public base including /api (getApiBase normalizes if missing)
VITE_API_URL=http://localhost:3001/api
# Solo DEV: usar mocks en lugar de /api/transit/* (default off)
# VITE_USE_TRANSIT_MOCKS=true
# Solo en desarrollo local. Nunca true en prod. Requiere import.meta.env.DEV.
# VITE_DEV_BYPASS_AUTH=true
